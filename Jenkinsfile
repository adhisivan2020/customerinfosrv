pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo 'running build'
			}
		}
		stage('Test') {
			steps {
				echo 'running test'
			}
		}
		stage('Deploy') {
			steps {
				echo 'running deployment'
			}		
		}
	}
}